package com.api.content_management.repository;

import com.api.content_management.model.entity.ComponentText;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComponentTextRepository extends JpaRepository<ComponentText, Long> {
}
